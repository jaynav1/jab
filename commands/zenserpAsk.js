const fetch = require("node-fetch")
const Discord = require ('discord.js')

module.exports = {
	name: 'ask',
	description: 'get results from google using zenserp',
	execute(client, receivedMessage, arguments) {
        
	}
}